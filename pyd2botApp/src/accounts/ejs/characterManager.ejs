<!DOCTYPE html>
<html>

    <head>
        <meta charset="UTF-8" />
        <title>Grider</title>
        <!-- Bootstrap CSS -->
        <link rel="stylesheet" href="<%= nodeModulesUrl + '/bootstrap/dist/css/bootstrap.min.css'%>" />
        <!-- Custom CSS -->
        <link rel="stylesheet" href="<%= cssUrl + '/main.css'%>"/>
        <!-- Bootstrap Optional JavaScript -->
        <!-- jQuery first, then Popper.js, then Bootstrap JS -->
        <script>let $ = require('jquery');</script>
        <script>require('popper');</script>
        <script>require('bootstrap');</script>
        <script defer src="render.js"></script>
    </head>

<body>
    <div class="wrapper">
        <%- include(sidebarUrl, {active: 'characters' }) %>
        <div class="bodyRightView">
            <ul class="nav nav-tabs" style="background: #262626">
                <li class="nav-item"><a class="nav-link active" aria-current="page" href="#">Characters</a></li>
                <li class="nav-item"><a class="nav-link" aria-current="page" href="<%=accounts.urls.manageAccountsUrl%>">Accounts</a></li>
            </ul>
            <table class="table table-dark table-striped" style="margin-bottom: 0;">
                <thead>
                    <th scope="col">Name</th>
                    <th scope="col">Id</th>
                    <th scope="col">serverId</th>
                    <th scope="col">accountId</th>
                    <th scope="col">action</th>
                </thead>
                <tbody>
                    <% Object.values(accounts.charactersDB).forEach(character => { %>
                    <tr>
                        <td scope="row"><%= character.characterName%></td>
                        <td><%= character.characterId%></td>
                        <td><%= character.serverId%></td>
                        <td><%= character.accountId%></td>
                        <td>
                            <div class="dropdown">
                                <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                                  <li><a class="dropdown-item" onclick="goToCharacterProfile('<%= character.characterId%>')">edit</a></li>
                                  <li><a class="dropdown-item" href="#" onclick="deleteCharacter('<%= character.characterId%>')">delete</a></li>
                                  <li><a class="dropdown-item" href="#" onclick="">run</a></li>
                                </ul>
                            </div>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
            <div class="managerBottomButtonsBar">
                <a href="<%=accounts.urls.newCharacterUrl%>"><button class="btn btn-primary" >new</button></a>
                <button class="btn btn-primary" onclick="saveCharacters()">save</button>
                <button class="btn btn-primary" onclick="clearCharacters()">clear</button>
            </div>
        </div>
    </div>
</body>

</html>